#
# Bundled Docker image defaults: idle until you mount a config with enabled lanes.
# See ../../examples/cotbridge.ini for a multicast-to-TAK example (use compose profile hostnet).
#

[cotbridge]
DEBUG = false
